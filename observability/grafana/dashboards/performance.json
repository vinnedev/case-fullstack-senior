{
  "uid": "relay-performance",
  "title": "Performance Investigation",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "10s",
  "time": {
    "from": "now-5m",
    "to": "now"
  },
  "tags": [
    "relay",
    "performance"
  ],
  "panels": [
    {
      "id": 1,
      "type": "timeseries",
      "title": "Pool API: utilização e espera",
      "gridPos": {
        "x": 0,
        "y": 0,
        "w": 12,
        "h": 8
      },
      "datasource": {
        "type": "prometheus",
        "uid": "prometheus"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "percentunit",
          "max": 1
        },
        "overrides": []
      },
      "targets": [
        {
          "refId": "A",
          "expr": "api_db_pool_utilization_ratio",
          "legendFormat": "utilização"
        },
        {
          "refId": "B",
          "expr": "api_db_pool_requests_waiting",
          "legendFormat": "aguardando"
        }
      ]
    },
    {
      "id": 2,
      "type": "timeseries",
      "title": "Tempo de queries por queryid",
      "gridPos": {
        "x": 12,
        "y": 0,
        "w": 12,
        "h": 8
      },
      "datasource": {
        "type": "prometheus",
        "uid": "prometheus"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "ms"
        },
        "overrides": []
      },
      "targets": [
        {
          "refId": "A",
          "expr": "topk(10, pg_query_top_mean_exec_time)",
          "legendFormat": "{{queryid}}"
        }
      ]
    },
    {
      "id": 3,
      "type": "table",
      "title": "Top queries por tempo total",
      "gridPos": {
        "x": 0,
        "y": 8,
        "w": 24,
        "h": 12
      },
      "datasource": {
        "type": "postgres",
        "uid": "postgres-relay"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "options": {
        "showHeader": true
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "rawSql": "SELECT queryid, calls, round(total_exec_time::numeric, 2) AS total_ms, round(mean_exec_time::numeric, 2) AS mean_ms, round(blk_read_time::numeric, 2) AS read_ms, round(blk_write_time::numeric, 2) AS write_ms, rows, shared_blks_read, shared_blks_hit, left(query, 500) AS query FROM pg_stat_statements WHERE dbid = (SELECT oid FROM pg_database WHERE datname = current_database()) ORDER BY total_exec_time DESC LIMIT 25"
        }
      ]
    },
    {
      "id": 4,
      "type": "table",
      "title": "Queries aguardando lock e bloqueadores",
      "gridPos": {
        "x": 0,
        "y": 20,
        "w": 24,
        "h": 12
      },
      "datasource": {
        "type": "postgres",
        "uid": "postgres-relay"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "options": {
        "showHeader": true
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "rawSql": "SELECT blocked.pid AS blocked_pid, round(extract(epoch FROM now() - blocked.query_start)::numeric, 1) AS waiting_seconds, left(blocked.query, 300) AS blocked_query, blocking.pid AS blocking_pid, left(blocking.query, 300) AS blocking_query FROM pg_stat_activity blocked JOIN pg_locks blocked_lock ON blocked_lock.pid = blocked.pid AND NOT blocked_lock.granted JOIN pg_locks blocking_lock ON blocking_lock.locktype = blocked_lock.locktype AND blocking_lock.database IS NOT DISTINCT FROM blocked_lock.database AND blocking_lock.relation IS NOT DISTINCT FROM blocked_lock.relation AND blocking_lock.page IS NOT DISTINCT FROM blocked_lock.page AND blocking_lock.tuple IS NOT DISTINCT FROM blocked_lock.tuple AND blocking_lock.virtualxid IS NOT DISTINCT FROM blocked_lock.virtualxid AND blocking_lock.transactionid IS NOT DISTINCT FROM blocked_lock.transactionid AND blocking_lock.classid IS NOT DISTINCT FROM blocked_lock.classid AND blocking_lock.objid IS NOT DISTINCT FROM blocked_lock.objid AND blocking_lock.objsubid IS NOT DISTINCT FROM blocked_lock.objsubid AND blocking_lock.pid <> blocked_lock.pid JOIN pg_stat_activity blocking ON blocking.pid = blocking_lock.pid WHERE NOT blocking_lock.granted ORDER BY waiting_seconds DESC"
        }
      ]
    },
    {
      "id": 5,
      "type": "timeseries",
      "title": "Tempo não atribuído a I/O por query (aproximação CPU/espera)",
      "gridPos": {
        "x": 0,
        "y": 32,
        "w": 24,
        "h": 8
      },
      "datasource": {
        "type": "prometheus",
        "uid": "prometheus"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "ms"
        },
        "overrides": []
      },
      "targets": [
        {
          "refId": "A",
          "expr": "topk(10, pg_query_top_total_exec_time - pg_query_top_blk_read_time - pg_query_top_blk_write_time)",
          "legendFormat": "{{queryid}}"
        }
      ]
    },
    {
      "id": 6,
      "type": "stat",
      "title": "Queries bloqueadas agora",
      "gridPos": {
        "x": 0,
        "y": 40,
        "w": 6,
        "h": 4
      },
      "datasource": {
        "type": "postgres",
        "uid": "postgres-relay"
      },
      "fieldConfig": {
        "defaults": {},
        "overrides": []
      },
      "targets": [
        {
          "refId": "A",
          "format": "table",
          "rawSql": "SELECT count(*) AS bloqueadas FROM pg_stat_activity WHERE wait_event_type = 'Lock'"
        }
      ]
    }
  ]
}
