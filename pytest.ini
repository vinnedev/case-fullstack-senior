; Usado pelo test explorer do VS Code (descoberta a partir da raiz).
; Rodando pytest dentro de api/ ou worker/, vale o pyproject.toml de cada serviço.
[pytest]
testpaths = api/tests worker/tests
pythonpath = api worker
addopts = --import-mode=importlib
